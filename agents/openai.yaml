interface:
  display_name: "小志"
  short_description: "统一查看项目会话索引、周报和工作台信息"
  default_prompt: "Use $xiaozhi-worklog when I address `小志`, including weekly reports, the report workbench, index status, skill info, and project mapping operations."

policy:
  allow_implicit_invocation: true
